using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TimelineLoader : MonoBehaviour
{
    [SerializeField] private TimelineObject timelineToLoad;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
